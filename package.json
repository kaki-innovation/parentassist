{
  "name": "parentassist",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "eval:prompts": "turbo run eval:prompts",
    "rag:ingest": "turbo run rag:ingest",
    "images:upload": "turbo run images:upload",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.6.3",
    "@types/node": "^22.9.0",
    "prettier": "^3.3.3",
    "eslint": "^9.15.0"
  },
  "packageManager": "pnpm@11.1.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
